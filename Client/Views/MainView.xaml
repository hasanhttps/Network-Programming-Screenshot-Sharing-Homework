<Window x:Class="Client.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client.Views"
        mc:Ignorable="d"
        Title="MainView" Height="1080" Width="1920">
    <Grid>
        <StackPanel>

            <Image Source="{Binding ImageData, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Height="860" Width="1720">
                
            </Image>

            <Button Command="{Binding ScreenshotButtonCommand}" Content="Take Screenshot" Width="150" Height="40" HorizontalAlignment="Center"/>

        </StackPanel>
    </Grid>
</Window>
